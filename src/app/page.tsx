import { ForensicHero } from '@/components/landing/ForensicHero'

// Public landing page (Forensic Dark hero). The audit app lives at /app.
export default function Home() {
  return <ForensicHero />
}
